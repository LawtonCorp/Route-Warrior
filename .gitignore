# Generated by XcodeGen from project.yml — never commit the project file.
*.xcodeproj

# Build products
.build/
build/
DerivedData/

# Your signing team, read by scripts/device-build.sh. Deliberately not in
# project.yml: CI builds unsigned and no developer account belongs in the repo.
scripts/signing.local

# macOS noise
.DS_Store

# SwiftPM local state
.swiftpm/
